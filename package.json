{
	"name": "handstage",
	"type": "module",
	"scripts": {
		"build": "tsdown --workspace && bunx tsc -p pkgs/dom/tsconfig.build.json --emitDeclarationOnly && bunx tsc -p pkgs/core/tsconfig.json --emitDeclarationOnly && bunx tsc -p pkgs/agent/tsconfig.json --emitDeclarationOnly",
		"test": "bun test"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.6",
		"@types/bun": "1.3.14",
		"tsdown": "^0.22.14",
		"typescript": "^7.0.2"
	},
	"peerDependencies": {
		"typescript": "^7.0.2"
	},
	"workspaces": [
		"pkgs/*"
	]
}
